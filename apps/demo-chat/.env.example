# Demo Chat App Environment Configuration Template
# Copy this to .env.local and configure for your environment

# TrueNamePath API Base URL (the main OAuth provider)
# IMPORTANT: This should be the TrueNamePath API port (usually 3000), NOT the demo-chat port!
VITE_API_BASE_URL=http://localhost:3000

# Demo Chat App URL (this app itself)
# This is where the demo-chat app runs (usually 4500)
VITE_APP_URL=http://localhost:4500

# OAuth App Name (must match registration in TrueNamePath API)
VITE_APP_NAME=demo-chat

# Development Port (this app runs on this port)
VITE_DEV_PORT=4500